todolist:
1.看文章做ppt
2.改系统的UI over
3.设计一个river
4.像一个design

整体架构：
1.进入main函数读入图片数据、论文数据
2.然后attributeF函数,计算3个属性的数量并显示到面板
3.给每个关键词添加click事件，每当点击，重新绘制面板（关键词指每个属性的关键词）
4.3个不同的属性都一个选择集合，最终的集合为这3个集合的并集
5.目前的bug在删除关键词上面，没有办法根据当前选择集（指3个属性的交集）进行删除，只能按照关键词重新索引删除，这样非常慢。
(解决方法:在第一次计算全集的时候，存储每个关键词对应的文章数据)

可以解决的问题（还没有做）：
1.目前文章的唯一的，coauthor和cokeyword没有重复向数据集加入。
2.最后的ans可以问文章id。
3.目前对于没有论文的年份是忽略的。
4.对于计算每个圆的位置，也可以放到外边。


关于可视化界面的步骤：
1.计算一个包含分组情况的数组。
2.计算半径
3.计算路径并平滑。
{
	1.计算一个保存path的数组。
}
看看ignore管用吗
